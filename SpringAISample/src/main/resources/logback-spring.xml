<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Console appender for development -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender for application logs -->
    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/chatbot.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Rollover daily or when file size reaches 10MB -->
            <fileNamePattern>logs/chatbot.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender for chat interactions -->
    <appender name="CHAT_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/chat-interactions.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/chat-interactions.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>90</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{sessionId:-N/A}] [%X{tenantId:-N/A}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender for training activities -->
    <appender name="TRAINING_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/training-activities.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/training-activities.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>25MB</maxFileSize>
            <maxHistory>60</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender for security/audit logs -->
    <appender name="SECURITY_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/security-audit.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/security-audit.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>15MB</maxFileSize>
            <maxHistory>365</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{userId:-N/A}] [%X{ip:-N/A}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Async appenders for performance -->
    <appender name="ASYNC_ROLLING_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="ROLLING_FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>80</discardingThreshold>
    </appender>

    <appender name="ASYNC_CHAT_LOG" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="CHAT_LOG"/>
        <queueSize>1024</queueSize>
        <discardingThreshold>80</discardingThreshold>
    </appender>

    <appender name="ASYNC_TRAINING_LOG" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="TRAINING_LOG"/>
        <queueSize>256</queueSize>
        <discardingThreshold>80</discardingThreshold>
    </appender>

    <appender name="ASYNC_SECURITY_LOG" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="SECURITY_LOG"/>
        <queueSize>256</queueSize>
        <discardingThreshold>80</discardingThreshold>
    </appender>

    <!-- Logger configurations -->
    <logger name="com.springaisample" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_ROLLING_FILE"/>
    </logger>

    <logger name="com.springaisample.service.ChatService" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_ROLLING_FILE"/>
        <appender-ref ref="ASYNC_CHAT_LOG"/>
    </logger>

    <logger name="com.springaisample.service.RagTrainingService" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_ROLLING_FILE"/>
        <appender-ref ref="ASYNC_TRAINING_LOG"/>
    </logger>

    <logger name="com.springaisample.service.RateLimiter" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_ROLLING_FILE"/>
        <appender-ref ref="ASYNC_SECURITY_LOG"/>
    </logger>

    <logger name="org.springframework.security" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_SECURITY_LOG"/>
    </logger>

    <logger name="org.springframework.ai" level="INFO" additivity="false">
        <appender-ref ref="ASYNC_ROLLING_FILE"/>
    </logger>

    <logger name="org.springframework.data" level="INFO" additivity="false">
        <appender-ref ref="ASYNC_ROLLING_FILE"/>
    </logger>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_ROLLING_FILE"/>
    </root>
</configuration>
